// TODO: Testimonials are intentionally EMPTY.
// Do NOT invent reviews. Add real customer testimonials here once Sensei Squeegee
// has collected them (e.g. from Google reviews, with permission).
// Shape: { quote: '“...”', name: 'First L.', location: 'Suburb', service: ['window'] }
// While this array is empty, the testimonial slider does not render at all.
const allTestimonials = [];

export default allTestimonials;
